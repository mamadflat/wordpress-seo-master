{
  "name": "seokav",
  "version": "1.3.0",
  "private": true,
  "description": "A privacy-first, multi-site SEO operations dashboard for WordPress and WooCommerce.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "npx --yes serve . -l 4173 --no-clipboard",
    "dev:api": "node server/index.mjs",
    "check": "node scripts/validate.mjs",
    "build:connector": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-connector.ps1",
    "check:api": "node --check server/index.mjs",
    "test:api": "node --test scripts/api.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
